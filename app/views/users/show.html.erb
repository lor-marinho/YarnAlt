


<% if(@user.id == current_user.id) %>
  <h2>My favourites yarns</h2>

  <% @fav = Favourite.where(user: current_user) %>
  <% @fav.each do |fav| %>
    <%= render 'shared/yarn', yarn: fav.yarn %>
  <% end %>
<% else %>
  <h1>Acesso Negado!</h1>
<% end %>
